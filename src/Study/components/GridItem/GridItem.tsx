// libraries
import React from 'react';
import { Level } from '../../helpers/imc';

// JS
import {
    Inside,
} from './gridItemStyles';

const GridItem: React.FC = () => {
    type Props ={
        item: Level
    };

    return (
        <Inside>...</Inside>
    );
};

export default GridItem;
